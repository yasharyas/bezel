{
  "name": "@bezel/registry",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "generate": "node scripts/generate.mjs",
    "check:registry": "node scripts/generate.mjs --check",
    "lint": "node scripts/generate.mjs --check && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
